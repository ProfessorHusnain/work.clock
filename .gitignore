# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/dist
/dist-release

# build resources (keep the directory, ignore specific files if needed)
# /build - Don't ignore, it contains installer resources

# Electron builder output
*.exe
*.dmg
*.pkg
*.deb
*.rpm
*.snap
*.AppImage
*.tar.gz
*.zip
*.blockmap
latest*.yml
alpha*.yml
beta*.yml

# Code signing certificates (NEVER COMMIT THESE!)
*.pfx
*.p12
*.cer
*.crt
*.key
*.keychain
build/certificate.*
build/*.pfx
build/*.p12

# macOS code signing
*.provisionprofile
embedded.provisionprofile

# Build resources that may contain sensitive info
build/installer.nsh
build/*-scripts/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# compiled output
.swc/
